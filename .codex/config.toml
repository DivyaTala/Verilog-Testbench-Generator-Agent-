model = "gpt-5.4"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"
model_reasoning_effort = "high"

[windows]
sandbox = "elevated"
# sandbox = "unelevated"  # Use this only if you do not have admin setup permissions

[features]
shell_snapshot = true
